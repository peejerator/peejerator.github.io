{
  "name": "peejerator-portfolio",
  "version": "1.0.0",
  "description": "Patrick Canady — CS / EE engineer portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^8.1.0"
  }
}
